<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hinsliu.iotapp.dal.message.MqttMessageDao">

    <insert id="insert" parameterType="com.hinsliu.iotapp.domain.model.message.DeviceMessageDO">
        insert into t_device_message (client, info, value, alert, longitude, latitude, timestamp)
        values ( #{clientId}, #{info}, #{value}, #{alert}, #{lng}, #{lat}, #{timestamp} )
    </insert>

</mapper>